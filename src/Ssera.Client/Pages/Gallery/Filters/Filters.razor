@using Ssera.Client.Infra.Components
@using Ssera.Shared.Data
@using Ssera.Shared.Images.Filters

<RadzenStack Orientation="Orientation.Horizontal" Wrap="FlexWrap.Wrap">

    <SseraFieldset Title="Order by">
        <RadzenStack Orientation="Orientation.Vertical">
            <RadzenDropDown
                Data="@_allOrderByTypes"
                TextProperty="@nameof(KeyValuePair<,>.Key)"
                ValueProperty="@nameof(KeyValuePair<,>.Value)"
                @bind-Value="@_filters.OrderByType"
            />

            <RadzenDropDown
                Data="@_allSortTypes"
                TextProperty="@nameof(KeyValuePair<,>.Key)"
                ValueProperty="@nameof(KeyValuePair<,>.Value)"
                @bind-Value="@_filters.SortType"
            />
        </RadzenStack>
    </SseraFieldset>

    <SseraFieldset Title="Eras" Variant="@_designVariant">
        <SseraMultiSelectCheckBoxList
            Data="@_allEras"
            @bind-Values="@_filters.Eras"
            ColumnCount="3"
        />
    </SseraFieldset>

    <SseraFieldset Title="Members" Variant="@_designVariant">
        <SseraMultiSelectCheckBoxList
            Data="@_allMembers"
            @bind-Values="@_filters.Members"
            ColumnCount="3"
         />
    </SseraFieldset>

    <!-- TODO: filtering based on date -->
    <!-- TODO: filtering for only vertical or only horizontal images -->

    <SseraFieldset Title="Tags" Style="">
        <RadzenStack Orientation="Orientation.Vertical">

            <RadzenDropDown
                @bind-Value="_filters.TagsSelectionType"
                Data="@_allTagsSelectionTypes"
                TextProperty="@nameof(KeyValuePair<,>.Key)"
                ValueProperty="@nameof(KeyValuePair<,>.Value)"
            />

            <RadzenDropDown
                TValue="IEnumerable<string>"
                Data="@_tagsDropdownData"
                @bind-Value="@_filters.Tags"
                Placeholder="Select tags"
                Multiple="true"
                AllowFiltering="true"
                AllowClear="true"
                AllowSelectAll="true"
                LoadData="@TagsDropdownLoadData"
            />

        </RadzenStack>
    </SseraFieldset>

    <RadzenStack Orientation="Orientation.Vertical">

        <SseraFieldset Title="Page size">
            <RadzenDropDown
                Data="@_allPageSizes"
                @bind-Value="@_filters.PageSize"
            />
        </SseraFieldset>

        <RadzenButton
            Click="ApplyFilters"
            Text="Apply filters"
            ButtonStyle="ButtonStyle.Primary"
            Style="height: min-content;"
            Disabled="@Loading"
        />

    </RadzenStack>

</RadzenStack>
